# Iterative speedup r03: fresh presolved correction LP and adaptive hour budgets

The user explicitly authorized multiple iterations and said to continue, superseding
the prior stop-after-one-run restriction. Every configuration is still separately
registered, component-tested, frozen and executed cold exactly once. This prevents
accidental duplicate runs; it is not an approval gate between improvements.
Continue from evidence if an attempt fails, rather than repeatedly requesting
permission. Preserve all prior results and the complete GO3 model/checks.

## Evidence and changes

The preceding `2275850` attempt failed after 582.535678 s. Its correction LP
import was verified, but HiGHS used a basis and skipped presolve after receiving
a complete infeasible primal start. It returned Unknown in 0.658188 s with zero
iterations and huge intermediate numerical values. The fixed 12-second Ipopt
fallback improved residual 84 to 24.7566, then the 45-second hourly rule stopped
the run despite substantial global time remaining. This was not proof of source
case infeasibility; the saved successful baseline remains 6,277.023909 s and
606,322,317.416927 verified objective.

This variant changes candidate generation/control only:

- Use a fresh native HiGHS correction LP, presolve explicitly on, with no primal
  or basis start supplied. Keep the current same-attempt AC point for nonlinear
  linearization/line search and complete Ipopt initialization. Retain import
  readback and native logs. Audit any returned LP point on the original unmodified
  linearization before evaluating the original nonlinear equations. Log whether
  presolve was observed and reject an unexpected useful-basis presolve bypass.
- Recompute hourly allowance from remaining refinement time divided by remaining
  hours, multiplied by 1.5, normally at least 60 s and never over 600 s. Preserve
  up to 20 s for each future hour (reduced to fair share if already time-poor),
  and never exceed the original absolute refinement deadline. This is allocation,
  not a promise that future hours can finish within 20 s.
- Initial linear corrections receive at most 45 s and 30% of the allocated hour;
  each LP receives at most 15 s and there are at most eight rounds. Import, oracle
  construction, JIT and audits consume these allowances; no clock is reset.
- If still infeasible, a fixed-shunt Ipopt fallback receives up to 120 native
  seconds, bounded by 75% of the then-remaining hour (less a two-second buffer),
  and at most 600 iterations. It can stop early only through the unchanged
  independently audited local feasibility guard. Optional source/same-attempt
  shunt revisiting still rounds before acceptance and may use a second fixed-shunt
  fallback from the remaining allocation. Worse candidates are rolled back.
- Emit phase/allocation diagnostics and avoid querying Ipopt for inapplicable
  MILP bounds/gaps in fallback statistics. Never claim a full GO3 bound from
  scheduling or local correction.

No model-side coefficient scaling, original-bound changes, source cost/penalty
changes, omitted contingencies, GPU, commercial solver, external solution, prior
full-run basis/dual, or new hour parallelism is introduced. Candidate topology
and transformer controls remain source-fixed restrictions; base balance slacks
remain zero only in the physical candidate search. Official permitted soft
penalties and final verification remain unchanged. No full-sized warmup or
separate replay of the failed correction matrix is performed.

## Registration and gates

`speedup_n06049_s003_r03`: C3E4N06049D2 scenario 003, 48 one-hour intervals,
3,902 source contingencies/hour, 187,296 required checks. Raw SHA256 remains
`312b066e51e4e058d0f2e1a96ea32c8f30a3f1dbfc9c4266b6d5d73af4206e41`.
Two-hour end-to-end ceiling, 30-minute soft target, 450 s reserved for final
independent/official verification and 30 s for serialization. Final reserve
allocation retains its 90-second protected work allowance. Scheduling remains
cold source-only HiGHS simplex with relative gap 1e-3, four HiGHS threads, and
one Julia/BLAS thread on the same laptop.

Success still requires all 48 hours and 187,296 checks, independent hard
feasibility and objective agreement, official feas=1 and phys_feas=1, and score
at least 537,717,593.314181 (90% of sixth-best 597,463,992.571312). Report soft
target attainment separately. Full solutions and failed-attempt evidence stay
local; compact provenance/results are pushed. Compare to the saved baseline,
not to a newly repeated baseline and not to an unmeasured DAYZER timing.

## Frozen component gate

Before the competition-sized attempt, all 29 component stages passed: 80 Python
tests, 979 Julia assertions, and nine independently/officially verified complete
tiny pipelines. The correction fixture confirms fresh LP import/readback, no
supplied primal or basis, preserved original domains/objective, audited returned
linearization residuals, original nonlinear residual acceptance, and exhaustive
fixture contingency coverage. Adaptive budget tests cover expired deadlines,
future-hour protection, fixed-policy compatibility and remaining-time fallback
allocation. These are component tests, not full-sized warmups.

- Component record SHA256:
  `9ee036c0460a4b1ec79d3f138b6ed96efe9c963c94f0d015d35a7cc65857c371`.
- Registered r03 configuration SHA256:
  `a2617a6fe8b0b19255dd60c281c22bbf68bb8b72d2a04b9be40751e072a57501`.
- Detailed local fixture evidence:
  `tmp/pilot002_component_gate_q6nck69k`.

## Completed cold attempt

Frozen numerical revision: `b711f12f586541447dfca90f03ad1ebeacf4c3e7`.
Run: `C3E4N06049D2_s003_speedup_n06049_s003_r03_20260918T231715Z`.
No live code, configuration or limits were changed during this attempt.

**FAIL: NO_VERIFIED_INCUMBENT**, after 734.898619 s through result serialization.
The original AC residual screen rejected hour 1; this was not the two-hour
deadline, and it does not prove the source case infeasible. Zero of 48 hours
passed and zero of 187,296 contingency-hour checks were completed. There is no
valid full-case objective, final dispatch comparison or new competition score.

| Quantity | Observed r03 value |
| --- | ---: |
| Scheduling stage | 504.969 s |
| Scheduling native HiGHS time | 342.164548 s |
| Scheduling objective / upper bound | 621,291,337.175008 / 621,357,577.274144 |
| Scheduling relative gap | 0.000106616807885 (0.0106617%) |
| Scheduling nodes / LP iterations | 1 / 422,173 |
| Initial joint-reserve reuse | 1.113 s |
| Failed AC-hour wall time | 195.342 s |
| First SLP phase / oracle construction | 27.902 / 10.152 s |
| First correction LP native time / iterations | 14.780942 s / 23,805 |
| First fixed-shunt fallback native / phase time | 114.299 / 121.504 s |
| First fallback iterations | 224 |
| Shunt revisit / rounded repair phase time | 5.360 / 18.279 s |
| Post-revisit fallback native / phase time | 3.938 / 8.849 s |
| Final maximum original-model residual | 1.16230023803 (required <= 1e-8) |
| Peak sampled process-tree RSS | 16.990631 GiB |
| Total through serialization | 734.898619 s (12 min 14.899 s) |

Times are nested, not additive. The controller's legacy field
`completed_ac_interval_wall_seconds` includes this failed interval; it must not
be read as one successfully completed AC hour. The scheduling objective and gap
are not a full AC/security objective or certificate.

All three native correction LPs passed the exact-domain/objective import audit,
ran presolve, used no supplied primal/basis, and exhausted their short time
allowances without a feasible returned point. Their native times/iterations were
14.780942/23,805, 2.319676/20,769 and 14.992057/33,506. No correction step was
accepted. The first presolve reduced 126,073 rows and 149,159 columns to 40,314
rows and 59,335 columns. The preceding immediate Unknown/useful-basis failure
did not recur; the current failure is not attributed to that resolved path.

The first fixed-shunt fallback reduced the audited residual from 84.0000000007
to 1.16230023803 but timed out. The brief shunt revisit changed no accepted
point. The final fallback was worse and was rolled back. The 190.452380-second
hour allocation was a soft phase allowance with ordinary return/audit overhead;
the separate end-to-end hard deadline was not approached. Merely increasing this
fixed-shunt fallback again is not established to work. The saved successful
baseline used a continuous-shunt candidate followed by a rounded-shunt repair;
this motivates testing that discrete-candidate ordering with the LP approach.

| Comparison | Full verified objective | End to end | Result |
| --- | ---: | ---: | --- |
| Saved `cfe7ea0` baseline | 606,322,317.416927 | 6,277.024 s | PASS, 48 hours and 187,296 checks |
| Previous `2275850` speedup | unavailable | 582.536 s | FAIL at AC hour 1 |
| This `b711f12` speedup | unavailable | 734.899 s | FAIL at AC hour 1 |

The shorter failed attempts are not speedups over a complete verified solve.
No baseline rerun or external-solution initialization was performed.

Hash-audited compact evidence is in
`evidence/speedup/speedup_n06049_s003_r03/`. All 60,016,800 bytes of full local
run evidence and candidates were preserved; zero files were deleted.

- Result SHA256:
  `e2ae27ffac6e55bc4f707cede404d095ec6881dff533f752a0fa8024237fe796`.
- Completion SHA256:
  `99f649b87d8e4a42cb176ce79a90546046e352452feb8a571a3036dfeeb5506c`.

The user has already authorized continued iterations. This failed gate does not
create a new approval requirement; any replacement must be newly registered,
component-tested, frozen and run cold while preserving these results.
