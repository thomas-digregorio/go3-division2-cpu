# Speedup r06: preserve within-attempt primal initialization through fallback

The user's continuing-iteration authorization applies. This is a newly
registered cold attempt following r05, not a duplicate or replacement of its
record. r05 evidence is archived and pushed at `e95e914`. Its numerical revision
`d2f44e4` passed hour 1 locally but failed hour 2 at residual 2.34072771978e-5.
There was no complete verified solution after 914.352973 s.

## Measured issue and focused correction

The preceding locally screened hour supplies a complete current-hour primal
start, with current source-domain projection and regenerated current-price
PWL blocks. `apply_ac_interval_start!` sets small interior pushes/fractions
(1e-8) and `mu_init=1e-6`. However, the correction's generic recovery rebuild
then reset them to 0.01 and 0.1. r05's hour-2 start record confirms those values.
That is a concrete mismatch between the advertised continuation and its actual
initialization. It is not evidence that the source case is infeasible.

New policy: `network_slp_preserved_primal_repair_v4`.

- Factor the existing primal-only continuation options into one helper and
  reapply them after a fallback rebuild when the current search originated
  from the previous locally screened hour in this attempt.
- Keep the complete current primal, including any accepted same-hour correction.
  Do not import another run or another hour's duals. This primal-only path still
  uses `warm_start_init_point=no`; its start is not a feasibility certificate.
- Where the r05 same-hour primal/dual repair validation succeeds, that genuine
  same-hour start continues to take precedence. The new rule does not replace it.
- Audit the actual native first Ipopt iterate, its full variable-identity map,
  and the maximum change from the requested start. Missing or invalid audit
  mappings fail explicitly rather than being called an accepted start.
- Keep r05's candidate-only early stop, discrete rounding, stricter 1e-10
  internal target and final 1e-8 original-model acceptance. No source PMIN/PMAX,
  other bound, cost, penalty, temporal/reactive/reserve constraint or contingency
  changes. First-hour ordinary initialization is unchanged apart from logging.

This correction targets robustness and avoids undoing useful initialization;
it does not promise the initial linearized IPX solves will become productive.
Those calls produced no accepted steps in r05. The full outcome and runtime
must be measured, with the unchanged final verification required.

## Scope, tests and benchmark

`config/speedup_n06049_s003_r06.json` differs from r05 only in attempt ID and
correction policy. Same raw SHA256
`312b066e51e4e058d0f2e1a96ea32c8f30a3f1dbfc9c4266b6d5d73af4206e41`, same 48 hours,
3,902 contingencies/hour and 187,296 checks; same cold simplex scheduling at
1e-3 gap, four HiGHS threads and one Julia/BLAS thread. Two-hour end-to-end
ceiling, 30-minute soft target, 450/30-second verification/serialization reserves,
and 90 seconds protected for final reserves remain unchanged.

Native tiny tests compare the actual first-iterate perturbation under ordinary
and preserved options. A separate synthetic fixture changes demand limits
between adjacent hours, forcing a genuine continuation repair; its complete
output must pass independent and official physical and exhaustive checks.
All older fixture paths remain in the full regression gate. No competition
data is used for component tests. Freeze and push only after that gate passes.

Use the saved 6,277.023909-second, 606,322,317.416927-score `cfe7ea0` baseline
without rerunning or initializing from it. The 90%-of-sixth-best threshold is
537,717,593.314181. This is a multi-change speedup comparison, not an isolated
ablation, global-optimality claim or matched DAYZER benchmark. The larger-network
campaign remains paused while iterating on this separate route.

## Focused validation

The new native-initialization regression passed all 24 assertions, and four
negative tests rejected disabled, incomplete, nonfinite or unobserved native
mapping audits. All 83 Python tests passed.

The full time-varying tiny pipeline at `tmp/r06_continuation_check_20260919`
passed independent hard feasibility, official feas=1/phys_feas=1, objective
agreement, and all nine contingency-hour checks. Reconstructed maximum P/Q
mismatches were 5.55111512313e-16 / 3.58368855424e-11 p.u. The actual native
first-iterate maximum movement was 0.01 for the cold ordinary first hour,
1e-8 for the preserved hour-2 primal start, and approximately 5e-9 for the
same-hour primal/dual rounded repair. The final tiny objective was
1361.042495117954; this is fixture evidence, not full-case performance.

Tiny solution SHA256:
`27b9cd6cfaee01eeef64f973551c179174dbdd68494337f0615751d02751e43e`.
The complete regression gate remains required before the cold full attempt.

## Full regression gate passed

`tmp/pilot002_component_gate_1rvx85jj` passed all 83 Python tests, 1,087 Julia
assertions and 35 subprocess stages. All 12 complete tiny pipelines passed
independent and official physical/exhaustive verification. The new time-varying
pipeline reproduced the exact focused-test solution hash above. Its audit
observed actual preserved initialization on hour 2, not merely an interface
attribute or a reused point that required no solve.

- Component manifest SHA256:
  `b06457bc335c34fe9dce21967d802c8c8b04a751053873cb86fc9089aecf05a0`.
- Configuration SHA256:
  `1cd22369b5b3407ab1bd530c9fee676d62a0d8204f945151e41af5e82f8786d2`.

Freeze this tested source/configuration inventory before the registered full
attempt. Tests used tiny original fixtures only; no competition-case warmup
or repetition was performed.
