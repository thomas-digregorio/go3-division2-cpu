# Speedup r07: trigger primal audits on original residuals

This is a distinct cold iteration under the user's continuing-run permission,
following the archived r06 failure at hour 18. Numerical r06 was `dd2ee07`;
its evidence/report was pushed in `1d27a5f`. The larger-network campaign remains
paused. No earlier solution, basis or dual vector is a starting point.

## Evidence and change

r06 completed 17 locally screened hours but failed rounded-shunt hour 18 at
5.05843742971e-6 after 1,993.860098 end-to-end seconds. The solver log had
displayed original-constraint violations around 4e-11 during earlier iterates,
yet the guard made zero complete audits. The guard wrongly used small callback
`inf_pr` as a necessary prerequisite to auditing original feasibility.

Ipopt documents that callback `inf_pr` describes the scaled internal problem,
including its inequality-slack representation, whereas the default printed
residual describes original constraints. The installed Ipopt.jl wrapper
supports `GetIpoptCurrentViolations(..., scaled=false, ...)` and throws on a
failed native request. See the
[official TNLP documentation](https://coin-or.github.io/Ipopt/classIpopt_1_1TNLP.html).
The unrecorded r06 intermediate points are not retrospectively certified.

New policy: `network_slp_original_residual_guard_v5`.

- Preserve all v4 initialization, mapping, same-hour dual repair and rounding.
- In the native callback, periodically query original unrelaxed bound and
  unscaled constraint violations. Readbacks must be dimensionally consistent,
  finite and nonnegative; missing native results fail explicitly.
- When that cheap query passes 1e-10, perform the existing complete mapped
  original-JuMP primal audit. Only that audit, plus the existing objective-window
  condition, can stop a solve. The query alone is not a certificate.
- Log both internal callback and original residuals, probe counts/time and
  whether the old internal gate would have rejected the audited stop.
- Preserve the 1e-10 internal audit target, 1e-8 final local tolerance, complete
  independent/official final checks and all source equations/domains/penalties.
  No KKT/global-optimality or dual-bound claim is made.
- Older policies retain their original callback trigger for regression tests.

The native tiny regression deliberately creates a feasible original inequality
while Ipopt's internal slack residual remains larger. Both legacy and current
JuMP expression paths now stop only after the complete audit; 34 assertions
pass. Four additional assertions reject a genuinely infeasible original model.
An invalid policy is also rejected. The full time-varying tiny pipeline and
all prior regression paths must pass before the registered full run.

## Frozen experiment contract

Configuration: `config/speedup_n06049_s003_r07.json`; authorization:
`manifests/authorization_speedup_007.json`. Relative to r06, only the attempt ID
and correction-policy ID change. Same immutable raw SHA256
`312b066e51e4e058d0f2e1a96ea32c8f30a3f1dbfc9c4266b6d5d73af4206e41`;
48 hours and 187,296 contingency-hour checks; cold simplex scheduling with
1e-3 gap, four HiGHS threads and one Julia/BLAS thread. Two-hour end-to-end hard
limit, 30-minute soft target and 450/30-second final verification/serialization
reserves. Existing hour allocations and native solve caps are unchanged.

Reference: saved `cfe7ea0`, 6,277.023909 s and verified score
606,322,317.416927. Required score: 537,717,593.314181 (90% of sixth-best).
Do not rerun or initialize from the reference. The overall speedup route is
a multi-change comparison, not an isolated ablation or matched DAYZER test.
Report actual complete outcome and quality tradeoff; earlier-hour feasibility
or a scheduling-subproblem bound cannot substitute for final verification.

## Full regression gate passed

`tmp/pilot002_component_gate_2e2h19pd` passed all 84 Python tests, 1,126 Julia
assertions and 37 subprocess stages. All 13 complete tiny pipelines passed
independent hard feasibility, official feas=1/phys_feas=1, objective agreement
and all nine fixture contingency-hour checks. No competition-case warmup ran.

The new changing-load pipeline exercised four actual nonlinear fallbacks and
15 original-residual probes, with four complete mapped audits and native
first-iterate readbacks. Its objective was 1361.042495117955; independently
reconstructed P/Q mismatches were 5.55111512313e-16 / 3.58368855424e-11 p.u.
The separate native inequality regressions demonstrated audited stops that
the old internal-residual trigger would reject, on both JuMP expression paths.
The integration fixture itself did not need that mismatch to stop.

- New tiny solution SHA256:
  `6414b7ba288afd29b566306ee431d58a4042fc753874575cd6cfe4f500ea4bb5`.
- Component manifest SHA256:
  `4fe3a02a3095bbaf2d037036592578f05b0b88f6b87b2abe591ae102ca4d4507`.
- Registered configuration SHA256:
  `743c8ea6af8b5fddb24a97fe45ef2014829018c0737e8eedc39c60e3e15d18bd`.

Freeze and push the exact tested source inventory before the registered cold
attempt. Component results are not evidence of full-case speed or feasibility.
